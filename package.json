{
  "name": "oauth-authorization",
  "version": "0.1.7",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "A set of elements that perform oauth authorization",
  "devDependencies": {
    "bower": "^1.8.0",
    "polymer-cli": "^1.2.0"
  },
  "private": false,
  "main": [
    "outh-authorization.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint oauth-authorization.html oauth1-authorization.html --rules polymer-1",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"oauth-authorization:local-test\"",
    "test": "node_modules/.bin/polymer test --plugin local",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/oauth-authorization.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/oauth-authorization/issues",
    "email": "arc@mulesoft.com"
  },
  "dependencies": {
    "http-proxy": "^1.16.2"
  }
}
